import OrderService from '../src/services/order.service';

describe('Order service tests', () => {
  it('should pass', () => {
    expect(2 + 2).toBe(4);
  });
  it('should fail', () => {
    expect(2 + 2).toBe(42);
  });
});
